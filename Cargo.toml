[package]
name = "telemetry-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.12", features = ["tokio", "canvas"] }
tokio = { version = "1", features = ["full"] }
socketcan = "3.5.0"
